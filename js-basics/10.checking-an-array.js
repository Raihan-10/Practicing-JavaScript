// 4. Checking if it's an Array
// Instructions:

// Create different variables, each containing either an array or a non-array value.

// Now use isArray to check if each variable is an array.

// Print a message to the console indicating whether each variable is an array or not.

let priotoma = ['sabrina', 'upoma', 'jaima', 'nilima'];
let meetingPoint = ['nsu','canteen', 'plaza area','studyhall'];
let biye = [];
let cheat;

console.log('Priotoma ase amr: '+Array.isArray(priotoma));

console.log('Meet korsi priotomader shathe: '+Array.isArray(meetingPoint));

console.log('Ami bibhito noi: '+Array.isArray(biye));
console.log('Ami cheat korsi? '+Array.isArray(cheat));
